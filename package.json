{
  "private": true,
  "type": "module",
  "scripts": {
    "build": "node build.mjs",
    "test": "node --test tests/collection.test.mjs tests/engine.test.cjs tests/flow.test.mjs",
    "dev": "npm run build && node server/preview.mjs"
  }
}
